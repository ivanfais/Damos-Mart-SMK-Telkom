# Backend API origin (no trailing slash, no /api/v1)
# Production (Railway) — sama dengan app Flutter default:
VITE_API_URL=https://damos-mart-smk-telkom-production.up.railway.app

# Lokal Laragon (opsional):
# VITE_API_URL=http://localhost:3000
