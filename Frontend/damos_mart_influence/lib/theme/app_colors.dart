import 'package:flutter/material.dart';

class AppColors {
  // Primary — identitas Damos Mart (hijau)
  static const Color primary       = Color(0xFF2E7D32);
  static const Color primaryLight  = Color(0xFF4CAF50);
  static const Color primaryDark   = Color(0xFF1B5E20);
  static const Color primarySurface = Color(0xFFE8F5E9); // hijau sangat muda

  // Accent — energik, menarik perhatian
  static const Color accent        = Color(0xFFFF9800); // oranye
  static const Color accentLight   = Color(0xFFFFB74D);

  // Background & Surface
  static const Color background    = Color(0xFFFFFFFF);
  static const Color surface       = Color(0xFFF5F5F5);
  static const Color card          = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary   = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint      = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color success       = Color(0xFF4CAF50);
  static const Color error         = Color(0xFFE53935);
  static const Color warning       = Color(0xFFFFA726);
  static const Color info          = Color(0xFF42A5F5);

  // Queue status
  static const Color queueWaiting    = Color(0xFFFFA726);
  static const Color queuePreparing  = Color(0xFF42A5F5);
  static const Color queueReady      = Color(0xFF4CAF50);
  static const Color queueCompleted  = Color(0xFF9E9E9E);

  // Stok
  static const Color inStock       = Color(0xFF4CAF50);
  static const Color outOfStock    = Color(0xFFE53935);

  // Divider & Border
  static const Color divider       = Color(0xFFE0E0E0);
  static const Color border        = Color(0xFFE0E0E0);

  // Shimmer
  static const Color shimmerBase   = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);
}
